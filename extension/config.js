// Update API_URL to your deployed web app URL after deploying to Vercel/etc.
// For local development, run `npm run dev` in web/ and use localhost.
const SPINCHECK_CONFIG = {
  API_URL: 'https://spincheck.vercel.app/api/analyze',
  // API_URL: 'http://localhost:3000/api/analyze', // uncomment for local dev
};
